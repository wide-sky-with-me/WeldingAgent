# Current Agent Session Goals

Date: 2026-05-25

## What We're Building
pWPS Agent: LLM-driven draft generation for welding procedures.
**Current Phase**: Stage One (autonomous draft + guided confirmation + checkpoint resume working)

## Next Priority Goals (in order)

### 1. ✅ DONE: Graph Runtime & Supervisor Loop
- LangGraph action loop with LLM Supervisor planning
- Structured `AgentAction` output and routing
- Loop protection for repeated actions
- Checkpoint/resume from latest state

### 2. ✅ DONE: Core Domain Tools
- requirement_understanding (LLM-backed)
- knowledge_planning (model-planned queries)
- web_search (Tavily + Brave support, caching, retry logic)
- field_reasoning (evidence → candidates)
- section_generation (A/B/C/D/E structured output)
- risk_report (missing fields, thermal/qualification flags)

### 3. ✅ DONE: Interaction Modes
- `auto_draft`: autonomous with visible uncertainty
- `guided_confirmation`: human-in-the-loop with evidence + confirmation history
- `supplement_update`: patch existing state and re-evaluate

### 4. ✅ DONE: Artifact Persistence
- pwps.json (state)
- pwps_draft.md (Markdown output)
- field_report.json (fields + evidence + risks)
- trace.json (action history)
- evidence_index.json (evidence ↔ field mappings)

### 5. IN PROGRESS: LLM-Led Dual Mode Hardening

Active plan: `docs/superpowers/plans/2026-05-25-llm-led-dual-mode-hardening.md`

The current target is not to reduce LLM involvement. The LLM Supervisor remains the main actor. The hardening work makes the two product modes explicit and auditable:

- `auto_draft`: only ask at the beginning when minimum core input is missing; then produce a draft with candidate/suggested/missing fields and risk notes.
- `guided_confirmation`: organize information, recommend options, explain applicability, and require human confirmation for key choices such as welding process, joint type, position, filler, shielding gas, preheat, and PWHT.

**Execution order:**

1. Initial information gate for `auto_draft`
2. Separate confidence, confirmation, and publishability
3. Evidence policy and promotion gate
4. Guided option recommendation tool
5. Split Supervisor policy from LLM planning
6. Agent eval metrics

**Cleanup already applied in this branch:**

- Removed old linear `run_auto_draft()` compatibility workflow
- Removed deterministic welding-content fallback that hard-coded ER50-6 candidates
- Added regression coverage that empty LLM field reasoning does not fabricate candidates

## Known Blockers / Dependencies
- None currently blocking Phase 8 work
- All core graph nodes, tools, and interaction modes are operational

## Test Baseline
```
142 passed in current run
Full test suite: ~2.43s
```

## Smoke Test Command
```bash
uv run pwps-agent auto-draft \
  "Q355B 12mm plate GMAW butt joint flat position AWS D1.1 pWPS draft" \
  --output-dir /tmp/pwps-smoke \
  --run-id smoke_$(date +%s)
```

Expected: Draft status `done`, visible uncertainty labels, evidence links, and quality/risk summaries.

## When Starting a Task
1. **Read**: AGENTS.md Current Implementation Progress (understand what's done)
2. **Check**: Applicable Domain Skills in `src/pwps_agent/domain_skills/`
3. **Look**: Existing tests for the module you're modifying
4. **Review**: `.instructions.md` working rules
5. **Test**: Full suite before commit

## When Finishing Work
- Update AGENTS.md line: "As of YYYY-MM-DD, the repository..."
- Add verification command and smoke output result
- Clean dead code/tests/debug output
- Commit with trailer: `Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>`
- Mark any phase/goal in this file as ✅ DONE

## Links
- Architecture: `docs/architecture.md`
- Design: `docs/agent_design.md`
- Active plan: `docs/superpowers/plans/2026-05-25-llm-led-dual-mode-hardening.md`
- Roadmap archive: `docs/superpowers/plans/2026-05-24-stage-one-completion-roadmap.md`
- Full Guidelines: `.instructions.md`
